<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mindaryn.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimeLine" />
        <activity android:name=".Memo" />
        <activity
            android:name=".IncomePopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".ExpensePopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".VariableCostPopup" />
        <activity android:name=".FixedCostPopup" />
        <activity android:name=".Step1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddProjectPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".DeleteProjectPopup"
            android:theme="@style/AppTheme.CustomTheme"/>
        <activity
            android:name=".ManangeProjectPopup"
            android:theme="@style/AppTheme.CustomTheme" />
    </application>

</manifest>